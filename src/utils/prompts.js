export const studyPlanPrompt = (topic) => `Create a personalized study plan for learning ${topic}.`;
export const quizPrompt = (topic) => `Generate a quiz for the topic: ${topic}.`;
export const interviewPrompt = (topic) => `Generate real interview-level questions for the topic: ${topic}.`; 